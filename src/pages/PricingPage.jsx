import React from 'react';
import { Check } from 'lucide-react';
import { navigate } from '../routes/AppRouter.jsx';
const plans=[{name:'Starter',price:'Coming soon',features:['Workspace setup','Properties','Cleaning and maintenance MVP']},{name:'Growth',price:'Coming soon',features:['Team roles','Private uploads','Operational workflows']},{name:'Scale',price:'Custom',features:['Advanced controls','Priority support','Future integrations']}];
export function PricingPage() { return <div className="public-page"><nav className="public-nav"><strong onClick={() => navigate('/')}>PropFlow</strong><button onClick={() => navigate('/login')}>Login</button></nav><section className="pricing-head"><h1>Pricing built for growing property operations</h1><p>Subscription plan selection is captured during workspace setup as a placeholder. Stripe billing is intentionally not implemented in Phase 1.</p></section><div className="pricing-grid">{plans.map((plan) => <article className="pricing-card" key={plan.name}><h3>{plan.name}</h3><strong>{plan.price}</strong><ul>{plan.features.map((feature) => <li key={feature}><Check size={16}/>{feature}</li>)}</ul><button className="primary" onClick={()=>navigate('/signup')}>Start {plan.name}</button><small className="todo">Billing checkout coming in a later phase.</small></article>)}</div></div>; }
