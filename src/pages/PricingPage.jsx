import React from 'react';
import { Check } from 'lucide-react';
import { plans } from '../data/sampleData.js';
import { navigate } from '../routes/AppRouter.jsx';
export function PricingPage() { return <div className="public-page"><nav className="public-nav"><strong onClick={() => navigate('/')}>PropFlow</strong><button onClick={() => navigate('/login')}>Login</button></nav><section className="pricing-head"><h1>Pricing built for growing property operations</h1><p>Subscriptions are stored at the workspace level with plan name, billing period, limits, and trial state. Stripe checkout buttons are placeholders for MVP.</p></section><div className="pricing-grid">{plans.map((plan) => <article className={`pricing-card ${plan.featured ? 'featured' : ''}`} key={plan.name}><h3>{plan.name}</h3><p>{plan.subtitle}</p><strong>{plan.price}<small>{plan.price !== 'Custom' && '/mo'}</small></strong><ul>{plan.features.map((feature) => <li key={feature}><Check size={16}/>{feature}</li>)}</ul><p className="limits">Properties: {plan.propertyLimit} · Users: {plan.userLimit}</p><button className="primary">Start {plan.name}</button><small className="todo">TODO: Replace with Stripe checkout session.</small></article>)}</div></div>; }
