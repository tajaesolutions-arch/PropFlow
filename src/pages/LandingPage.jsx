import React from 'react';
import { ArrowRight, Building2, ClipboardCheck, Wrench } from 'lucide-react';
import { navigate } from '../routes/AppRouter.jsx';
export function LandingPage() { return <div className="public-page"><nav className="public-nav"><strong>PropFlow</strong><div><button onClick={() => navigate('/pricing')}>Pricing</button><button onClick={() => navigate('/login')}>Login</button><button className="primary" onClick={() => navigate('/signup')}>Start free</button></div></nav><section className="hero"><div><span className="eyebrow">Property operations, finances, and teams in one workspace</span><h1>Premium property management SaaS for short-term and long-term rentals.</h1><p>PropFlow helps hosts, landlords, property managers, cleaners, maintenance crews, and owners run every property from a clean multi-workspace command center.</p><div className="hero-actions"><button className="primary large" onClick={() => navigate('/signup')}>Create workspace <ArrowRight size={18}/></button><button className="secondary large" onClick={() => navigate('/login')}>Login</button></div></div><div className="hero-panel"><div className="feature-row"><Building2/> Workspace-scoped property portfolio</div><div className="feature-row"><ClipboardCheck/> Cleaner checklists and before/after photos</div><div className="feature-row"><Wrench/> Maintenance work orders and urgent alerts</div></div></section></div>; }
