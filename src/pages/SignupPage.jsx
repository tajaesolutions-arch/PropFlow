import React from 'react';
import { currencies } from '../data/constants.js';
import { navigate } from '../routes/AppRouter.jsx';
export function SignupPage() { return <div className="auth-page"><div className="auth-card wide"><h1>Create your PropFlow workspace</h1><p>Signup supports new workspaces, invite links, and company codes. Invited users become active without manual approval unless suspended.</p><div className="form-grid"><label>Company / workspace name<input placeholder="Blue Harbor Stays" /></label><label>Country<input placeholder="Jamaica" /></label><label>Default currency<select>{currencies.map((c) => <option key={c}>{c}</option>)}</select></label><label>Signup path<select><option>Create a new workspace</option><option>Join existing workspace</option><option>Join by invite link</option><option>Join by workspace/company code</option></select></label></div><button className="primary" onClick={() => navigate('/dashboard')}>Create demo workspace</button><button onClick={() => navigate('/join')}>I have an invite or code</button></div></div>; }
