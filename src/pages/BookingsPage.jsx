import React from 'react';
import { AppLayout } from '../components/layout/AppLayout.jsx';
import { EmptyState } from '../components/EmptyState.jsx';
export function BookingsPage(){ return <AppLayout title="Bookings"><EmptyState title="Bookings are coming soon." description="Direct booking pages and external booking platform integrations are outside Phase 1. No fake booking records are shown." /></AppLayout>}
