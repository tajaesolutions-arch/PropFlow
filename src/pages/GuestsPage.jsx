import React from 'react';
import { AppLayout } from '../components/layout/AppLayout.jsx';
import { EmptyState } from '../components/EmptyState.jsx';
export function GuestsPage(){ return <AppLayout title="Guests / CRM"><EmptyState title="Guest CRM is coming soon." description="Direct booking pages and external booking integrations are outside Phase 1, so no fake guest data is shown." /></AppLayout>}
